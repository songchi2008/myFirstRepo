package com.mljr.spider.dao.impl;

import org.junit.After;
import org.junit.Assert;
import org.junit.Before;
import org.junit.Test;
import com.mljr.spider.model.YyUserCallRecordDo;

/**
 * @author ckex created 2016-11-29 16:14:49:049
 * @explain -
 */
public class YyUserCallRecordDaoImplTest {

    @Before
    public void testSetup() {
       // YyUserCallRecordDo record = new YyUserCallRecordDo();
       // record.setId(id);
       // record.setSellerId(sellerId);
       // record = yyUserCallRecordDao.create(record);
    }
    @After
    public void testTeardown() {
       // yyUserCallRecordDao.delete(id,sellerId);
    }
    @Test
    public void testLoad() {
       // YyUserCallRecordDo load = yyUserCallRecordDao.load(id,sellerId);
       // Assert.assertNotNull(load);
       // Assert.assertEquals("feiyingtest", load.getXxxx());
       // logger.debug(load.toString());
    }
    @Test
    public void testUpdate() {
       // YyUserCallRecordDo record = new YyUserCallRecordDo();
       // record.setId(id);
       // record.setSellerId(sellerId);
       // record.setXxxx("hellofeiying");
       // yyUserCallRecordDao.update(record);

       // YyUserCallRecordDo load = yyUserCallRecordDao.load(id,sellerId);
       // Assert.assertNotNull(load);
       // Assert.assertEquals("hellofeiying", load.getXxxx());
       // logger.debug(load.toString());
    }
    @Test
    public void testListByPage() {
        // common.page.util.PageQuery pageQuery = new common.page.util.PageQuery(0,10);
        // Integer count = null;
        // common.page.util.PageList<YyUserCallRecordDo> result =         yyUserCallRecordDao.listByPage(sellerId,pageQuery,count);
        // Assert.assertNotNull(result);
         // for (YyUserCallRecordDo bean : result) {
         //     logger.debug(bean.toString());
         // }
    }
}
